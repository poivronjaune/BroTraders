"""
brotoolsv2.strategies

Folder containing individual Strategy class files. Each strategy module
defines a Strategy class with an `active` flag; brotoolsv2.strategy_loader
discovers and loads every class here where active = True.
"""
